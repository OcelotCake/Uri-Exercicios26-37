function compareNumber(x , y){
    let largestNumber = Math.max(x, y);
  
    if(x > y || y > x) {
      return largestNumber;
    } 
    else if(x === y) {
      return "The numbers are equal";
    } 
    else{ 
      return false;
    }
  } 
  document.write(compareNumber(3, 3));
